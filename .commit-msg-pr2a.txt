feat(slice-1-pr2): wire dashboard page and access gate redirect

PR 2a: Dashboard Wiring (clean scope)

Changes:
- Wire /backbox page to DashboardStateMachine component
- Fetch entitlements and redirect NONE state to /access
- Implement /access page with reverse redirect logic
- Add access page translations (EN + FR)

Implementation:
- backbox/page.tsx: Fetches entitlements, redirects NONE→/access, renders state machine
- access/page.tsx: Shows minimal access gate UI, redirects non-NONE→/backbox
- Translations: Added access.title, access.description, access.cta.* keys

This PR contains ONLY the dashboard wiring scope per Slice 1 Plan.
Build/dependency fixes and React 19 compat are handled separately.

Files modified:
- apps/web/app/(saas)/backbox/page.tsx
- apps/web/app/(saas)/access/page.tsx
- packages/i18n/translations/fr.json
- packages/i18n/translations/en.json
