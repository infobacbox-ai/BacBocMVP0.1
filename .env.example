# Copy to .env and fill in values. Do not commit .env.

# Required for app + DB + mail + auth
DATABASE_URL=
RESEND_API_KEY=
BETTER_AUTH_SECRET=

# Optional: base URL (Better Auth, redirects). Defaults to http://localhost:3000
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: mail from address (defaults to noreply@supastarter.dev)
# MAIL_FROM=

# Optional: storage (MVP0 may not need)
# NEXT_PUBLIC_AVATARS_BUCKET_NAME=avatars

# Optional: payments (if enabling billing)
# LEMONSQUEEZY_API_KEY=
# DODO_PAYMENTS_API_KEY=
# CREEM_API_KEY=

# Optional: social login (if enabling)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
